---
// Layout básico sin estilos, ahora con colores lavanda pastel y modo oscuro personalizado
---
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Marcelo Aguirre - Desarrollador Full Stack</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
  </head>
  <body style="background: #E6E6FA; color: #222; transition: background 0.3s, color 0.3s;">
    <main>
      <slot />
    </main>
    <script>
      const lightBtn = document.getElementById('light-mode');
      const darkBtn = document.getElementById('dark-mode');
      function setLight() {
        document.body.style.background = '#E6E6FA';
        document.body.style.color = '#222';
      }
      function setDark() {
        document.body.style.background = '#3D3D14';
        document.body.style.color = '#E6E6FA';
      }
      if (lightBtn && darkBtn) {
        lightBtn.onclick = setLight;
        darkBtn.onclick = setDark;
      }
    </script>
  </body>
</html> 